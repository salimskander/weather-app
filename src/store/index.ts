import { createStore } from 'vuex';

export default createStore({
  state: {
    // État initial
  },
  mutations: {
    // Mutations
  },
  actions: {
    // Actions
  },
  modules: {
    // Modules
  }
});
